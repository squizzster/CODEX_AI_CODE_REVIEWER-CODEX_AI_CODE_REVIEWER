|
  
  If your final output is sometime of report, consider the ./outputs directory which is write-enabled.
  Also consider, ./scratch_pad for any in-turn notes, or ./temp_scripts if you need to code!
